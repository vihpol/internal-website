use_default_settings: true

general:
  instance_name: "MICAS Search"

search:
  safe_search: 1
  autocomplete: ""
  default_lang: "en"
  formats:
    - html
    - json

server:
  limiter: false
  image_proxy: false

ui:
  static_use_hash: true
  default_locale: "en"
